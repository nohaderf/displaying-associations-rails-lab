<%# index page lists songs in 'Artist Name - Song Title' format  %>
<h1>Songs</h1>
<ul>
  <% @songs.each do |song| %>
    <li>
        <a href="<%= song_path(song)%>">
        <%= song.artist.name %>
        - 
        <%= song.title %>
        </a>
    </li>
  <% end %>
</ul>

